syntax = "proto3";

message Orientation {
    float x = 1;
    float y = 2;
    float z = 3;
    float yaw = 4;
    float pitch = 5;
    float roll = 6;
}

service Orierntal {
    rpc Orient (Orientation) returns (Orientation);
}